mutation AddConference($name: String!, $city: String!) {
    addConference(conference: {
        name: $name,
        city: $city
    }) {
        id
        name
        city
        talks {
            id
        }
    }
}